// dokument object model
//representation of html doocument
// html tag, body tag anything which is in the document and we use JS to manipulate
// these douments
// DOM is its methods and properties that we can change
// browser object and window object
// root tag is html element and we have body tag or h1 tag( they are like siblings)
// use query selecter instead of jQuery( it is simplier and faster)